apiVersion: v1
kind: Service
metadata:
  name: student-service
spec:
  type: NodePort
  selector:
    run: student-service
  ports:
    - name: http
      protocol: TCP
      port: 7000
      targetPort: 7000
      nodePort: 30001
