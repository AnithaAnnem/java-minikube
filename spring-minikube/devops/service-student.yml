kind: Service
apiVersion: v1
metadata:
  name: student-service
spec:
  type: NodePort         # change here
  selector:
    run: student-service
  ports:
    - name: port1
      protocol: TCP
      port: 7000
      targetPort: 7000
      nodePort: 30001    # assign a free port
